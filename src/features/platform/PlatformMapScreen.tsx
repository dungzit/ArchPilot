import { ArrowUpRight } from 'lucide-react'
import { ModulePanel } from '../../ui/ModulePanel'
import { copy, type TextFn } from '../../ui/copy'

export function PlatformMap({ text }: { text: TextFn }) { return <ModulePanel eyebrow="PLATFORM WORKFLOW" title={text(copy('Nhìn technology theo capability.', 'See technology by capability.'))} description={text(copy('Tổ chức application, data, storage, network, security và DevOps platform mà không khóa vào một vendor.', 'Organize application, data, storage, network, security, and DevOps without locking into one vendor.'))}><div className="platform-grid">{[['Application', 'API · EKS · workers', 'teal'], ['Data', 'Aurora · PostgreSQL · Kafka', 'blue'], ['Storage', 'S3 · Ceph · backup', 'amber'], ['Networking', 'ALB · F5 · DNS · VLAN', 'coral'], ['Security', 'IAM · WAF · Vault · SIEM', 'navy'], ['DevOps', 'GitLab · Terraform · Ansible', 'green']].map(([title, items, tone]) => <section className={`panel platform-card ${tone}`} key={title}><div className="platform-symbol">{title.slice(0, 1)}</div><h2>{title}</h2><p>{items}</p><button className="text-button">Open capability <ArrowUpRight size={14} /></button></section>)}</div></ModulePanel> }
