import { ModulePanel } from '../../ui/ModulePanel'
import { copy, type TextFn } from '../../ui/copy'

export function Lifecycle({ text }: { text: TextFn }) { return <ModulePanel eyebrow="6R SOLUTION LIFECYCLE" title={text(copy('Một quyết định, sáu chặng.', 'One decision, six stages.'))} description={text(copy('Theo dõi từ request đến reassessment, để architecture không kết thúc ở bản vẽ.', 'Track the journey from request to reassessment, so architecture does not end at a diagram.'))}><div className="lifecycle-board">{['Request', 'Requirements', 'Review', 'Resolve', 'Realize', 'Reassess'].map((stage, index) => <div className={`life-stage ${index < 3 ? 'done' : index === 3 ? 'current' : ''}`} key={stage}><span>{String(index + 1).padStart(2, '0')}</span><strong>{stage}</strong><small>{index < 3 ? 'complete' : index === 3 ? 'in progress' : 'up next'}</small></div>)}</div><div className="module-grid"><section className="panel"><div className="eyebrow">CURRENT DECISION</div><h2>ADR-024 · Event backbone</h2><p className="muted-copy">Resolve trade-offs between managed streaming, portability, and five-year value.</p><div className="progress-line"><span style={{ width: '61%' }} /></div><div className="module-meta"><span>Decision progress</span><strong>61%</strong></div></section><section className="panel"><div className="eyebrow">OPEN ACTIONS</div><h2>3 gates before realize</h2><div className="compact-list"><span><i className="list-dot coral-dot" /> Attach load test evidence</span><span><i className="list-dot amber-dot" /> Confirm labor cost assumption</span><span><i className="list-dot teal-dot" /> Review rollback path</span></div></section></div></ModulePanel> }
