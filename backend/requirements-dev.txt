-r requirements.txt
# Test-only dependencies. httpx is required by fastapi.testclient.
# (jsonschema used to be listed here; since task 1.6 it is a runtime dependency
# in requirements.txt, because app/contract.py validates design bodies against
# ../contracts/archgraph.schema.json.)
pytest==8.3.4
httpx==0.28.1
# Lint-only (task 0.7). Configured by backend/ruff.toml, run by
# .github/workflows/ci.yml as `ruff check .`. `ruff format --check` is NOT in CI
# yet - see the note at the bottom of ruff.toml.
ruff==0.14.6
