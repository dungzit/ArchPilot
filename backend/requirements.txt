# ArchPilot backend - runtime dependencies.
# Deliberately minimal. Everything else (sqlite3, hashlib, secrets, json,
# logging) comes from the Python standard library.
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
